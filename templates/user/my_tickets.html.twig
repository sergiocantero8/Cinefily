{% extends 'base.html.twig' %}

{% block title %}Mis entradas | Cinefily{% endblock %}


{% block main %}

    <div class="container">
        <div class="main-body">
            <div class="row">
                <div class="col-lg-3">
                    {% include 'user/includes/data_profile.html.twig' %}
                </div>
                <div class="col-lg-9">
                    <div class="user-profile">
                        <div class="card-body">
                            <div class="h4"><i class="fas fa-ticket-alt pr-2"></i>Tus últimas 6 entradas</div>
                            <div class="row">
                                {% for ticket in tickets %}
                                    <div class="col-6 mt-3">
                                        <div class="h6 font-weight-bold">{{ ticket.ticket.getSaleDate|date('d/m/y H:i') }}</div>
                                        {% include 'session/includes/ticket_movie.html.twig' %}
                                    </div>
                                {% endfor %}
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>





{% endblock %}

    {% block custom_body_javascript %}

    {% endblock %}