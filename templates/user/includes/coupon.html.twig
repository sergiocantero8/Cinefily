<div class="container">
    <div class="coupon p-3">
        <div class="row">
            <div class="col-12">
                <div>
                    <div class="status-coupon">
                        {% if active%}
                            <span class="btn-success font-weight-bold">ON</span>
                        {% else %}
                            <span class="btn-danger font-weight-bold">OFF</span>
                        {% endif %}

                    </div>
                    <div class="d-flex flex-row justify-content-center off">
                        <h1>{{ discount }}%</h1>
                    </div>
                    <div class="d-flex flex-row justify-content-center p-2"><span class="h6 mr-1">Código:</span><span
                                class="border border-success px-3 rounded code h6">{{ code }}</span></div>
                </div>
            </div>
        </div>
    </div>
</div>